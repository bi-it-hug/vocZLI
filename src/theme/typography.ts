import { StyleSheet } from "react-native"

export const typography = StyleSheet.create({
    heading: {
        fontSize: 24,
        fontWeight: "bold",
    },
    body: {
        fontSize: 16,
    },
})
